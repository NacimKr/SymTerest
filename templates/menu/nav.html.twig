<nav class="navbar navbar-dark bg-dark navbar-expand-lg bg-body-tertiary">
  <div class="container">
    <a class="navbar-brand text-secondary fw-bold" href="{{ path('app_home') }}">Panterest</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

      <div class="navbar-nav">
        {% if app.user %}
          <a class="nav-link" href="{{ path('app_create') }}">New Pin</a>
          <a class="nav-link" href="{{ path('app_account') }}">Account</a>
          <a class="nav-link" href="#" onclick="event.preventDefault(); document.querySelector('#js-logout-form').submit()">Logout</a>

          <form id="js-logout-form" action="{{ path('app_logout') }}" method="post" style="display:none">
            <input type="hidden" name="csrf_token" value="{{ csrf_token('logout') }}">
          </form>
          {# OU #}
          {# <form id="js-logout-form" action="{{ path('app_logout') }}?_csrf_token={{ csrf_token('logout') }}" method="post" style="display:none">
            <input type="hidden" name="csrf_token" value="{{ csrf_token('logout') }}">
          {# OU #}
          {# <a class="nav-link" href="{{ path_logout(main) }}">Logout</a> #}
          </form> 
        {% else %}
          <a class="nav-link" href="{{ path('app_register') }}">Register</a>
          <a class="nav-link" href="{{ path('app_login') }}">Login</a>
        {% endif %}
      </div>
  </div>
</nav>